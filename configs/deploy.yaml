defaults:
  - _self_
  - robot_camera
  - tactile: paxini
  - task: bottle_opening
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

model: VINN
absolute_actions: false

encoder:
  name: resnet18
selected_view: 1

data_path: expert_dataset/openbottle_new_filtered/extracted_data # 

run_loop: false
loop_frequency: 15 # 1 move per second